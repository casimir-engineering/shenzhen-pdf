- One expand / collapse button, at the end of the chapter filter row

---

- The chapter list's Expand All / Collapse All pair is now a single button on the filter field's own row, showing the action it will perform. The list gets back the row of height the pair took.
- The readme now links a complete feature list (`docs/features.md`): every format, panel, shortcut and setting, plus the full Markdown syntax and diagram support tables.
